# Lab 5 — Device Twin: plain MQTT, anonymous access. This lab teaches the
# twin's desired/reported synchronization loop, not transport security —
# see Chapter 4's lab for mTLS on a broker just like this one.

listener 1883 0.0.0.0
allow_anonymous true

persistence false

log_dest stdout
log_type error
log_type warning
log_type notice
